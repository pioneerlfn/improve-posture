## tips
```shell
`$@`: target自身
`-B`: 无条件make
`-j4`: 并发make
`@`: 悄悄地执行，比如:
	`@echo test`只会打印出`test`, 而不会打印 `echo test`
`-n`:        -n, --just-print, --dry-run, --recon
            Print the commands that would be executed, but do not execute them.
```

